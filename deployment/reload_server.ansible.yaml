---
- hosts: cdn-dashboard
  become: yes
  name: Running process
  tasks:
    - name: Reload openresty proxy server
      ansible.builtin.command: 
        chdir: /home/nguyenquangminh/cdn_dashboard/
        cmd: openresty -c nginx.conf -p /home/nguyenquangminh/cdn_dashboard/ -s reload